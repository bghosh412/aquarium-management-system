# CO2 Regulator Node Configuration
# Format: KEY=VALUE (no spaces around =)

NODE_TANK_ID=0
NODE_NAME=UnmappedCO2
FIRMWARE_VERSION=1

# ESP-NOW Settings
ESPNOW_CHANNEL=11

# Debug Settings
DEBUG_SERIAL=true
DEBUG_ESPNOW=true
DEBUG_HARDWARE=false

# Timing Settings
ANNOUNCE_INTERVAL_MS=5000
HEARTBEAT_INTERVAL_MS=30000
CONNECTION_TIMEOUT_MS=90000
# Hub heartbeat timeout (ms) - if node does not receive hub heartbeat for this long, enter fail-safe
HUB_HEARTBEAT_TIMEOUT_MS=600000
