/**
 * Filesystem-only build stub
 * 
 * This file exists only to satisfy PlatformIO's build requirements
 * for filesystem-only environments (hub_esp32_staticfs, hub_esp32_userfs).
 * 
 * These environments are intended for "Build Filesystem Image" and 
 * "Upload Filesystem Image" operations only.
 * 
 * To build/upload filesystems, use:
 *   pio run -e hub_esp32_staticfs -t buildfs
 *   pio run -e hub_esp32_staticfs -t uploadfs
 *   pio run -e hub_esp32_userfs -t buildfs
 *   pio run -e hub_esp32_userfs -t uploadfs
 */

// Empty - no actual code needed
