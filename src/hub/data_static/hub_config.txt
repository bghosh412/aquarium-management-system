# Aquarium Management System - Hub Configuration
# One-off configuration parameters
# Format: KEY=VALUE (no spaces around =)

# Heartbeat Monitoring
HEARTBEAT_ENABLED=true
HEARTBEAT_INTERVAL_SEC=120

# Memory Management
AGGRESSIVE_MEMORY_MANAGEMENT=true
HEAP_WARNING_THRESHOLD_KB=50
PSRAM_WARNING_THRESHOLD_KB=100

# WiFi Settings
WIFI_AP_NAME=AquariumHub
WIFI_AP_PASSWORD=aquarium123
WIFI_TIMEOUT_SEC=180

# mDNS Configuration
MDNS_HOSTNAME=ams

# ESP-NOW Settings
ESPNOW_CHANNEL=11
ESPNOW_MAX_PEERS=20

# Hub heartbeat - broadcast interval in seconds
# Hub will broadcast a HEARTBEAT every HUB_HEARTBEAT_INTERVAL_SEC seconds (default 120)
# Hub heartbeat - broadcast interval in seconds
# Hub will broadcast a HEARTBEAT every HUB_HEARTBEAT_INTERVAL_SEC seconds (default 120)
HUB_HEARTBEAT_INTERVAL_SEC=120

# Test mode: when enabled, hub exposes debug/test-only APIs (force fail-safe)
# Set to true for automated integration tests only
HUB_TEST_MODE=false

# Debug Settings
DEBUG_SERIAL=true
DEBUG_ESPNOW=true
DEBUG_WEBSOCKET=false

# Scheduler sleep behavior
# When next task is far away, scheduler sleeps up to SCHEDULER_MAX_SLEEP_SEC
# When next task is near, scheduler sleeps at least SCHEDULER_MIN_SLEEP_SEC
SCHEDULER_MIN_SLEEP_SEC=30
SCHEDULER_MAX_SLEEP_SEC=300

# ============================================================================
# HUB OTA Update Configuration
# ============================================================================
# Base URLs for Hub firmware and LittleFS OTA updates
# These URLs should point to directories containing version.txt and the binary files
# version.txt should contain semantic version like: 1.0.0
HUB_FIRMWARE_OTA_URL=https://raw.githubusercontent.com/bghosh412/OTA/main/AMS/Hub/firmware/
HUB_LITTLEFS_OTA_URL=https://raw.githubusercontent.com/bghosh412/OTA/main/AMS/Hub/littleFS/

# Current installed versions (updated automatically after successful OTA)
# Format: major.minor.patch (e.g., 1.0.0)
HUB_FIRMWARE_VERSION=1.0.0
HUB_LITTLEFS_VERSION=1.0.0

# ============================================================================
# Node OTA Update Configuration
# ============================================================================
# Base URL for lighting node OTA updates
LIGHT_NODE_OTA_URL=https://raw.githubusercontent.com/bghosh412/OTA/main/AMS/Node/Light/

# ntfy.sh Notification
NTFY_TOPIC=ams-hub-bg
